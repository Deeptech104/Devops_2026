Here’s a **basic list of common Linux commands** with simple explanations 👇
(Perfect if you’re just getting started.)

---

## 📁 File & Directory Commands

* `ls` – list files and folders
  `ls -l` (detailed), `ls -a` (show hidden files)
* `pwd` – show current directory
* `cd` – change directory
  `cd /home/user` | `cd ..` (go back)
* `mkdir foldername` – create a folder
* `rmdir foldername` – delete empty folder
* `rm file` – delete file
  `rm -r folder` (delete folder & contents)

---

## 📄 File Viewing & Editing

* `cat file` – show file content
* `less file` – view file page by page
* `head file` – show first 10 lines
* `tail file` – show last 10 lines
* `nano file` – edit file (beginner friendly)
* `vi file` / `vim file` – advanced editor

---

## 📦 File Operations

* `cp src dest` – copy file
* `mv src dest` – move/rename file
* `touch file` – create empty file
* `stat file` – file details

---

## 🔐 Permissions

* `chmod 755 file` – change permissions
* `chown user:group file` – change owner
* `ls -l` – view permissions

---

## 🔍 Search & Find

* `find /path -name filename` – find files
* `grep "text" file` – search text in file
* `grep -r "text" folder` – recursive search

---

## 🖥️ System Info

* `uname -a` – system info
* `top` / `htop` – running processes
* `df -h` – disk usage
* `free -h` – memory usage
* `uptime` – system running time

---

## 📦 Package Management (Ubuntu/Debian)

* `sudo apt update`
* `sudo apt install package`
* `sudo apt remove package`

(RHEL/CentOS: `yum` or `dnf`)

---

## 🌐 Networking

* `ip a` – network interfaces
* `ping google.com` – check connectivity
* `curl url` – fetch data from web
* `wget url` – download files

---

## ❓ Help

* `man command` – manual page
* `command --help` – quick help

---

* curl -s -X GET https://api.sampleapis.com/beers/ale | jq .
*
